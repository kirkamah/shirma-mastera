// Removed — AI integration was rolled back per user request.
// Kept as empty stub because the project isn't a git repo, so deleting
// orphan files would be irreversible. Vite tree-shakes empty modules.
export {}
